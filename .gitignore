# Fichiers temporaires / visualisations (pas nécessaires dans le repo)
data/mnist_*.png
data/voir_mnist.py
data/npz2idx.py
data/mnist.npz

# EMNIST (données brutes, ~570 Mo avec le zip — à télécharger soi-même)
data/emnist/

# KMNIST (données brutes, ~21 Mo — à télécharger soi-même)
data/kmnist/

# Python caching
__pycache__/
*.pyc
*.pyo

# IDE
.vscode/
.idea/
tune_result.png
preview_samples.png
training_result.png
training_result_full.png
predictions.png

# Résultats EMNIST (générés)
emnist_samples.png
emnist_training_result.png
models/emnist_letters_weights*.npz

# Résultats KMNIST (générés)
kmnist_samples.png
kmnist_training_result.png
models/kmnist_weights*.npz

# Resultats adversarial (versionnes dans le repo depuis 2026-08-26)
# (les PNG sont generes par fgsm.py / pgd.py / transfer.py / defend.py)

# Images générées à la racine du projet (results/)
results/*.png
results/*.csv

# Résultats d'expérimentations (générés, pas dans le repo)
experiments/*/tune_result.png
experiments/*/model_weights.npz
experiments/*/training_result.png
experiments/*/*.png
experiments/*/*_weights.npz

# Benchmark / résultats numériques
experiments/*/benchmark*.txt
experiments/*/results*.csv

# Points de reprise de harden2 (gros, regenerables) : on ne versionne que le meilleur
models/*_last.npz
adversarial/results/logs/*
!adversarial/results/logs/.gitkeep

# Piste PyTorch : checkpoints et modeles (lourds, regenerables)
models/*.pt
models/*_last.pt
